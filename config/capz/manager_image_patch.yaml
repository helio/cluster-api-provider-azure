apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: eu.gcr.io/k8s-helio-development/cluster-api-azure-controller-amd64:debug-1.14.2-v2
        name: manager
